import { PreventKeyDirective } from './prevent-key.directive';

describe('PreventKeyDirective', () => {
  it('should create an instance', () => {
    const directive = new PreventKeyDirective();
    expect(directive).toBeTruthy();
  });
});
